import styled from "@emotion/styled";

export const PuffStitchFiveDoubleCrochetContainer =
  styled.svg((props: any) => ({
    stroke: `${props.color}`,
    strokeWidth: `${props.strokeWidth}`,
    cursor: "pointer",
  }));
